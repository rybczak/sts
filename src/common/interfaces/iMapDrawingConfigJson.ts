"use strict";

export interface IMapDrawingConfigJson {
    fps: number;

    mapHeight: number;

    mapWidth: number;

    screenHeight: number;

    screenWidth: number;

    viewPortHeight: number;

    viewPortWidth: number;

    movementSize: number;

    menuTileSize: number;
}